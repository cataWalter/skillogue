import 'package:flutter/material.dart';
import 'package:test/test.dart';
import 'package:skillogue/entities/profile.dart';
/*
void main(){
  group('Profile', (){

    test('Empty profile', (){
      final profile = Profile('', '', '', '', '', 1, DateTime.now(), Colors.white,
          [], []);
      bool profile_status = profile.isEmptyProfile();
      expect(profile_status, true);
    });

    test('Not empty profile', (){
      final profile = Profile('Test1', 'Test1', 'Test1', 'Test1', 'Test1', 1, DateTime.now(), Colors.white,
          ['test1'], ['test1']);
      bool profile_status = profile.isEmptyProfile();
      expect(profile_status, false);
    });
  });

}*/